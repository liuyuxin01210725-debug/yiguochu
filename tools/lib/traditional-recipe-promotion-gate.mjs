export const PROMOTION_MATRIX = new Map([
  ['shanghai-salted-pork-vegetable-rice', { draft_id: 'shanghai-salted-pork-vegetable-rice-draft', candidate_id: 'shanghai-salted-pork-vegetable-rice', family_id: 'family-jiangnan-vegetable-rice' }],
  ['suzhou-salted-pork-vegetable-rice', { draft_id: 'suzhou-salted-pork-vegetable-rice-draft', candidate_id: 'suzhou-salted-pork-vegetable-rice', family_id: 'family-jiangnan-vegetable-rice' }],
  ['nanjing-cured-pork-greens-rice', { draft_id: 'nanjing-cured-pork-greens-rice-draft', candidate_id: 'nanjing-cured-pork-greens-rice', family_id: 'family-jiangnan-vegetable-rice' }],
  ['nanjing-sausage-greens-rice', { draft_id: 'nanjing-sausage-greens-rice-draft', candidate_id: 'nanjing-sausage-greens-rice', family_id: 'family-jiangnan-vegetable-rice' }],
  ['nanjing-duck-greens-rice', { draft_id: 'nanjing-duck-greens-rice-draft', candidate_id: 'nanjing-duck-greens-rice', family_id: 'family-jiangnan-vegetable-rice' }],
  ['jinshan-clay-oven-vegetable-rice', { draft_id: 'jinshan-clay-oven-vegetable-rice-draft', candidate_id: 'jinshan-clay-oven-vegetable-rice', family_id: 'family-jiangnan-vegetable-rice' }],
  ['taiwan-cabbage-mushroom-rice', { draft_id: 'taiwan-cabbage-mushroom-rice-draft', candidate_id: 'taiwan-cabbage-mushroom-rice', family_id: 'family-southern-savory-rice' }],
  ['quanzhou-oil-rice', { draft_id: 'quanzhou-oil-rice-draft', candidate_id: 'quanzhou-oil-rice', family_id: 'family-southern-savory-rice' }],
  ['fujian-gai-cai-minced-pork-rice', { draft_id: 'fujian-gai-cai-minced-pork-rice-draft', candidate_id: 'fujian-gai-cai-minced-pork-rice', family_id: 'family-southern-savory-rice' }],
  ['fujian-hyacinth-bean-rice', { draft_id: 'fujian-hyacinth-bean-rice-draft', candidate_id: 'fujian-hyacinth-bean-rice', family_id: 'family-southern-savory-rice' }],
  ['hainan-cai-bao-rice', { draft_id: 'hainan-cai-bao-rice-draft', candidate_id: 'hainan-cai-bao-rice', family_id: 'family-southern-savory-rice' }],
  ['daxi-lotus-leaf-oil-rice', { draft_id: 'daxi-lotus-leaf-oil-rice-draft', candidate_id: 'daxi-lotus-leaf-oil-rice', family_id: 'family-southern-savory-rice' }],
  ['cantonese-cured-meat-claypot-rice', { draft_id: 'cantonese-cured-meat-claypot-rice-draft', candidate_id: 'cantonese-cured-meat-claypot-rice', family_id: 'family-covered-pot-rice' }],
  ['cantonese-mushroom-chicken-claypot-rice', { draft_id: 'cantonese-mushroom-chicken-claypot-rice-draft', candidate_id: 'cantonese-mushroom-chicken-claypot-rice', family_id: 'family-covered-pot-rice' }],
  ['cantonese-black-bean-pork-rib-claypot-rice', { draft_id: 'cantonese-black-bean-pork-rib-claypot-rice-draft', candidate_id: 'cantonese-black-bean-pork-rib-claypot-rice', family_id: 'family-covered-pot-rice' }],
  ['xinjiang-lamb-pilaf', { draft_id: 'xinjiang-lamb-pilaf-draft', candidate_id: 'xinjiang-lamb-pilaf', family_id: 'family-northwest-grain-rice' }],
  ['xinjiang-vegetable-pilaf', { draft_id: 'xinjiang-vegetable-pilaf-draft', candidate_id: 'xinjiang-vegetable-pilaf', family_id: 'family-northwest-grain-rice' }],
  ['shaanbei-red-date-cowpea-rice', { draft_id: 'shaanbei-red-date-cowpea-rice-draft', candidate_id: 'shaanbei-red-date-cowpea-rice', family_id: 'family-northwest-grain-rice' }],
  ['qinghai-hao-fan', { draft_id: 'qinghai-hao-fan-draft', candidate_id: 'qinghai-hao-fan', family_id: 'family-northwest-grain-rice' }],
  ['shanxi-potato-rice', { draft_id: 'shanxi-potato-rice-draft', candidate_id: 'shanxi-potato-rice', family_id: 'family-northwest-grain-rice' }],
  ['shanxi-nitun-millet-rice', { draft_id: 'shanxi-nitun-millet-rice-draft', candidate_id: 'shanxi-nitun-millet-rice', family_id: 'family-northwest-grain-rice' }],
  ['tibetan-savory-congee', { draft_id: 'tibetan-savory-congee-draft', candidate_id: 'tibetan-savory-congee', family_id: 'family-regional-grain-specialties' }],
  ['tibetan-gutu', { draft_id: 'tibetan-gutu-draft', candidate_id: 'tibetan-gutu', family_id: 'family-regional-grain-specialties' }],
  ['tibetan-ginseng-fruit-rice', { draft_id: 'tibetan-ginseng-fruit-rice-draft', candidate_id: 'tibetan-ginseng-fruit-rice', family_id: 'family-regional-grain-specialties' }],
  ['guangxi-five-color-glutinous-rice', { draft_id: 'guangxi-five-color-glutinous-rice-draft', candidate_id: 'guangxi-five-color-glutinous-rice', family_id: 'family-regional-grain-specialties' }],
  ['she-people-black-rice', { draft_id: 'she-people-black-rice-draft', candidate_id: 'she-people-black-rice', family_id: 'family-regional-grain-specialties' }],
  ['banshan-wild-rice', { draft_id: 'banshan-wild-rice-draft', candidate_id: 'banshan-wild-rice', family_id: 'family-regional-grain-specialties' }],
  ['dai-pineapple-purple-rice', { draft_id: 'dai-pineapple-purple-rice-draft', candidate_id: 'dai-pineapple-purple-rice', family_id: 'family-regional-grain-specialties' }],
  ['guizhou-dong-community-rice', { draft_id: 'guizhou-dong-community-rice-draft', candidate_id: 'guizhou-dong-community-rice', family_id: 'family-regional-grain-specialties' }],
  ['north-china-green-bean-braised-noodles', { draft_id: 'north-china-green-bean-braised-noodles-draft', candidate_id: 'north-china-green-bean-braised-noodles', family_id: 'family-northern-braised-noodles' }],
]);

const IDENTITY_PLACEHOLDER = /经核验|身份不明|未知野菜|地方植物/u;
const CANONICAL_ORIGIN = 'https://yiguochu.pages.dev';
const CANONICAL_BASE = 'https://yiguochu.pages.dev/recipes.html?id=';

// Drafts deliberately use generic or review-gated ingredient language. A promoted
// recipe may resolve one of those terms only to these concrete, narrower foods.
const DRAFT_TERM_NARROWINGS = new Map(Object.entries({
  '精白米': ['大米'],
  '食品级大米': ['大米'],
  '米饭': ['大米'],
  '大米或杂粮': ['大米', '小米', '燕麦米'],
  '食品级谷物': ['大米', '小米'],
  '食品级小麦粉面团': ['小麦面团'],
  '清水': ['水'],
  '少量咸肉': ['咸五花肉'],
  '咸肉': ['咸五花肉'],
  '腊肉': ['腊五花肉'],
  '腌肉': ['腊五花肉'],
  '切片香肠': ['广式腊肠'],
  '来源可追溯的熟制鸭可食部分': ['包装熟制板鸭（去骨）'],
  '腊味肉制品': ['广式腊肠'],
  '可追溯鸡肉可食部分': ['去皮鸡腿肉'],
  '可追溯猪肋排': ['猪肋排'],
  '去骨猪肉可食部分': ['去骨猪腿肉'],
  '羊肉块': ['羊腿肉', '羊肩肉'],
  '猪肉': ['猪肉末', '去皮鸡腿肉', '老豆腐'],
  '鸡腿肉': ['去皮鸡腿肉'],
  '肉末': ['猪肉末'],
  '肉末或老豆腐': ['猪肉末', '老豆腐'],
  '猪肉或老豆腐': ['猪肉末', '老豆腐'],
  '叶菜': ['小白菜', '菜心', '小油菜', '矮脚黄'],
  '青菜': ['矮脚黄', '小油菜', '小白菜'],
  '普通叶菜': ['小白菜', '油麦菜'],
  '有明确食品名称的叶菜': ['小白菜'],
  '经核验可食用的地方叶菜': ['油麦菜'],
  '经核验可食用叶片': ['生菜'],
  '蔬菜馅': ['胡萝卜'],
  '已核验蔬菜': ['小白菜', '胡萝卜'],
  '新鲜食用菌': ['鲜香菇', '白玉菇', '平菇', '口蘑'],
  '经核验的新鲜食用菌': ['鲜香菇'],
  '食用香菇': ['鲜香菇'],
  '香菇': ['鲜香菇'],
  '有明确食品名称的新鲜平菇': ['平菇'],
  '有明确食品名称的新鲜白玉菇': ['白玉菇'],
  '有明确食品名称的食用菌': ['鲜香菇'],
  '食品级香菇': ['鲜香菇'],
  '有食品标签的香菇': ['鲜香菇'],
  '有食品标签的鲜香菇': ['鲜香菇'],
  '有食品标签的平菇': ['平菇'],
  '已核验豆类': ['熟鹰嘴豆'],
  '有食品标签的鹰嘴豆': ['熟鹰嘴豆'],
  '有食品标签的白芸豆': ['熟白芸豆'],
  '熟豆类': ['熟鹰嘴豆'],
  '经声明的乳制品或非乳替代品': ['牛奶'],
  '有明确过敏原标签的牛奶': ['牛奶'],
  '有明确成分标签的非乳替代品': ['无糖豆浆（有明确成分标签）'],
  '有明确食品名称的干果': ['红枣'],
  '有食品标签的红枣': ['红枣'],
  '有食品标签的葡萄干': ['葡萄干'],
  '经审核的水果或坚果': ['葡萄干'],
  '已识别的食品级植物色源': ['食品级紫薯粉'],
  '单一已核验食品级植物色源': ['食品级黑米色粉'],
  '有明确食品标签的可食用色源': ['食品级甜菜粉'],
  '经食品级核验的可食用植物浸提物': ['食品级甜菜粉'],
  '有食品标签的可食用植物色源': ['食品级紫薯粉'],
  '经核验的可食用配料': ['椰奶'],
  '有明确食品名称的坚果或种子': ['南瓜子'],
  '已核验可食野生食材': ['鲜香菇', '平菇'],
  '经核验可食用的新鲜水果': ['芒果'],
  '椰浆或其他椰子制品': ['椰奶'],
  '香辛料': ['姜'],
  '修整豆角': ['豆角'],
  '食品级豆豉': ['豆豉'],
  '食品级清洁荷叶': ['食品级干荷叶'],
  '经核验可食用的食品级叶片包材': ['食品级白菜叶'],
}));

function entries(value, key) {
  return Array.isArray(value?.[key]) ? value[key] : [];
}

function indexById(items) {
  return new Map(items.map(item => [item?.id, item]));
}

function duplicateErrors(promotions, key) {
  const seen = new Set();
  const errors = [];
  for (const promotion of promotions) {
    const value = promotion?.[key];
    if (seen.has(value)) errors.push(`promotion manifest duplicates ${key} ${value}`);
    else seen.add(value);
  }
  return errors;
}

function findIdentityPlaceholder(value) {
  if (typeof value === 'string') return IDENTITY_PLACEHOLDER.test(value) ? value : undefined;
  if (Array.isArray(value)) {
    for (const item of value) {
      const placeholder = findIdentityPlaceholder(item);
      if (placeholder) return placeholder;
    }
  }
  if (value && typeof value === 'object') {
    for (const item of Object.values(value)) {
      const placeholder = findIdentityPlaceholder(item);
      if (placeholder) return placeholder;
    }
  }
  return undefined;
}

function recipeIdentityPlaceholder(recipe) {
  return findIdentityPlaceholder([
    recipe?.core_ingredients,
    recipe?.optional_ingredients,
    recipe?.generation_optional_ingredients,
    recipe?.generation_liquid_ingredients,
    recipe?.generation_boundaries,
    recipe?.substitution_slots,
  ]);
}

function hasNonEmptyString(value) {
  return typeof value === 'string' && value.trim().length > 0;
}

function hasCanonicalSource(recipe, canonicalPath) {
  const expectedUrl = `${CANONICAL_ORIGIN}${canonicalPath}`;
  return entries(recipe, 'source_refs').some(ref => (
    ref?.usage === 'approved'
      && ref?.url === expectedUrl
      && hasNonEmptyString(ref?.title)
      && hasNonEmptyString(ref?.license)
      && hasNonEmptyString(ref?.attribution)
      && hasNonEmptyString(ref?.retrieved_at)
  ));
}

function expectedCanonicalPath(recipeId) {
  return `/recipes.html?id=${recipeId}`;
}

function sameArray(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}

function termIsSameOrNarrower(draftTerm, productionTerm) {
  return draftTerm === productionTerm
    || (DRAFT_TERM_NARROWINGS.get(draftTerm) || []).includes(productionTerm);
}

function productionTermMatchesAny(draftTerms, productionTerm) {
  return Array.isArray(draftTerms)
    && draftTerms.some(draftTerm => termIsSameOrNarrower(draftTerm, productionTerm));
}

function impossibleIngredientActions(technique) {
  if (!Array.isArray(technique)) return [];
  const impossible = [
    /牛奶[^。]*(?:切|切块|切丁)/u,
    /荷叶[^。]*(?:切|切碎|切丁|作为主料[^。]*(?:煮|焖|炒))/u,
    /(?:植物色源|色粉|紫薯粉|黑米粉)[^。]*(?:切|切碎|切丁|作为主料[^。]*(?:煮|焖|炒))/u,
  ];
  return technique.filter(step => impossible.some(pattern => pattern.test(step)));
}

function validateDraftSemantics(recipeId, draft, recipe) {
  const errors = [];
  for (const field of ['core_ingredients', 'optional_ingredients']) {
    const draftTerms = Array.isArray(draft?.[field]) ? draft[field] : [];
    const productionTerms = Array.isArray(recipe?.[field]) ? recipe[field] : [];
    for (const term of productionTerms) {
      if (!productionTermMatchesAny(draftTerms, term)) {
        const label = field === 'core_ingredients' ? 'core' : 'optional';
        errors.push(`${recipeId} production ${label} ingredient is outside draft semantics: ${term}`);
      }
    }
    for (const term of draftTerms) {
      if (!productionTerms.some(value => termIsSameOrNarrower(term, value))) {
        const label = field === 'core_ingredients' ? 'core' : 'optional';
        errors.push(`${recipeId} production ${label} ingredients do not resolve draft term: ${term}`);
      }
    }
  }

  const draftSlots = Array.isArray(draft?.substitution_slots) ? draft.substitution_slots : [];
  const recipeSlots = Array.isArray(recipe?.substitution_slots) ? recipe.substitution_slots : [];
  if (recipeSlots.length !== draftSlots.length) {
    errors.push(`${recipeId} substitution slot count must equal linked draft`);
  }
  for (const draftSlot of draftSlots) {
    const recipeSlot = recipeSlots.find(slot => slot?.slot === draftSlot?.slot);
    if (!recipeSlot) {
      errors.push(`${recipeId} missing draft substitution slot ${draftSlot?.slot}`);
      continue;
    }
    const boundary = [
      ...(Array.isArray(recipe.core_ingredients) ? recipe.core_ingredients : []),
      ...(Array.isArray(recipe.optional_ingredients) ? recipe.optional_ingredients : []),
    ];
    for (const term of Array.isArray(recipeSlot.replaces) ? recipeSlot.replaces : []) {
      if (!productionTermMatchesAny(draftSlot.replaces, term)) {
        errors.push(`${recipeId} substitution slot ${draftSlot.slot} replaces ingredient outside draft semantics: ${term}`);
      }
      if (!boundary.includes(term)) {
        errors.push(`${recipeId} substitution slot ${draftSlot.slot} replaces ingredient outside production boundary: ${term}`);
      }
    }
    for (const term of Array.isArray(recipeSlot.allowed) ? recipeSlot.allowed : []) {
      if (!productionTermMatchesAny(draftSlot.allowed, term)) {
        errors.push(`${recipeId} substitution slot ${draftSlot.slot} allows ingredient outside draft semantics: ${term}`);
      }
    }
  }

  if (!sameArray(recipe?.technique, draft?.technique_outline)) {
    errors.push(`${recipeId} technique must retain the linked draft technique outline`);
  }
  if (!sameArray(recipe?.ratio_rules, draft?.draft_ratio_rules)) {
    errors.push(`${recipeId} ratio_rules must retain the linked draft ratio rules`);
  }
  const safetyRules = Array.isArray(recipe?.safety_rules) ? recipe.safety_rules : [];
  for (const gate of Array.isArray(draft?.safety_and_quality_gates) ? draft.safety_and_quality_gates : []) {
    if (hasNonEmptyString(gate?.requirement) && !safetyRules.includes(gate.requirement)) {
      errors.push(`${recipeId} safety_rules missing draft requirement: ${gate.requirement}`);
    }
  }
  for (const step of impossibleIngredientActions(recipe?.technique)) {
    errors.push(`${recipeId} has impossible ingredient action: ${step}`);
  }
  return errors;
}

export function hasOnlyExpectedMissingProductionErrors(errors, promotions) {
  const promotionEntries = Array.isArray(promotions) ? promotions : promotions?.promotions;
  if (!Array.isArray(errors) || errors.length === 0 || !Array.isArray(promotionEntries)) return false;
  const expectedErrors = new Set(promotionEntries.map(
    promotion => `${promotion?.recipe_id} missing production recipe`,
  ));
  return errors.every(error => expectedErrors.has(error));
}

export function validateTraditionalRecipePromotion({ candidates, drafts, production, promotions, matrix = PROMOTION_MATRIX }) {
  const errors = [];
  const candidateEntries = entries(candidates, 'entries');
  const draftEntries = entries(drafts, 'drafts');
  const productionEntries = entries(production, 'recipes');
  const promotionEntries = Array.isArray(promotions) ? promotions : promotions?.promotions;
  const actualPromotions = Array.isArray(promotionEntries) ? promotionEntries : [];
  const expectedMatrix = matrix instanceof Map ? matrix : PROMOTION_MATRIX;

  if (actualPromotions.length !== expectedMatrix.size) {
    errors.push(`promotion manifest must contain exactly ${expectedMatrix.size} promotions`);
  }
  for (const key of ['recipe_id', 'draft_id', 'candidate_id']) {
    errors.push(...duplicateErrors(actualPromotions, key));
  }

  const promotionsByRecipeId = new Map(actualPromotions.map(promotion => [promotion?.recipe_id, promotion]));
  for (const [recipeId, expected] of expectedMatrix) {
    const promotion = promotionsByRecipeId.get(recipeId);
    if (!promotion) {
      errors.push(`expected promotion ${recipeId} is missing`);
      continue;
    }
    if (promotion.draft_id !== expected.draft_id) errors.push(`${recipeId} must use draft_id ${expected.draft_id}`);
    if (promotion.candidate_id !== expected.candidate_id) errors.push(`${recipeId} must use candidate_id ${expected.candidate_id}`);
    if (promotion.family_id !== expected.family_id) errors.push(`${recipeId} must use family_id ${expected.family_id}`);
  }
  for (const promotion of actualPromotions) {
    if (!expectedMatrix.has(promotion?.recipe_id)) errors.push(`unexpected promotion ${promotion?.recipe_id} is present`);
  }

  const candidatesById = indexById(candidateEntries);
  const draftsById = indexById(draftEntries);
  const productionById = indexById(productionEntries);
  for (const promotion of actualPromotions) {
    const recipeId = promotion?.recipe_id;
    const canonicalPath = promotion?.canonical_path;
    const requiredCanonicalPath = expectedCanonicalPath(recipeId);
    const candidate = candidatesById.get(promotion?.candidate_id);
    const draft = draftsById.get(promotion?.draft_id);
    const recipe = productionById.get(recipeId);

    if (!candidate) errors.push(`${recipeId} missing candidate ${promotion?.candidate_id}`);
    else if (candidate.status !== 'candidate') errors.push(`${recipeId} linked candidate ${promotion.candidate_id} must have status candidate`);
    if (!draft) errors.push(`${recipeId} missing draft ${promotion?.draft_id}`);
    else {
      if (draft.status !== 'draft') errors.push(`${recipeId} linked draft ${promotion.draft_id} must have status draft`);
      if (draft.candidate_id !== promotion.candidate_id) errors.push(`${recipeId} draft candidate_id must equal ${promotion.candidate_id}`);
    }
    if (canonicalPath !== requiredCanonicalPath) {
      errors.push(`${recipeId} canonical_path must equal ${requiredCanonicalPath}`);
    }
    if (!recipe) {
      errors.push(`${recipeId} missing production recipe`);
      continue;
    }
    if (recipe.status !== 'approved') errors.push(`${recipeId} production status must be approved`);
    if (recipe.family_id !== promotion.family_id) {
      errors.push(`${recipeId} production family_id must equal manifest ${promotion.family_id}`);
    }
    if (recipe.cuisine !== promotion.cuisine) {
      errors.push(`${recipeId} production cuisine must equal manifest ${promotion.cuisine}`);
    }
    if (!sameArray(recipe.purposes, promotion.purposes)) {
      errors.push(`${recipeId} production purposes must equal manifest purposes`);
    }
    if (recipe.total_time_minutes !== promotion.total_time_minutes) {
      errors.push(`${recipeId} production total_time_minutes must equal manifest ${promotion.total_time_minutes}`);
    }
    if (recipe.origin_candidate_id !== promotion.candidate_id) {
      errors.push(`${recipeId} origin_candidate_id must equal ${promotion.candidate_id}`);
    }
    if (draft) errors.push(...validateDraftSemantics(recipeId, draft, recipe));
    if (!hasNonEmptyString(recipe.adaptation_note)
      || !recipe.adaptation_note.includes(promotion.identity_resolution)) {
      errors.push(`${recipeId} adaptation_note must retain manifest identity resolution`);
    }
    const placeholder = recipeIdentityPlaceholder(recipe);
    if (placeholder) errors.push(`${recipeId} contains identity placeholder ${placeholder}`);
    if (canonicalPath === requiredCanonicalPath && !hasCanonicalSource(recipe, canonicalPath)) {
      errors.push(`${recipeId} canonical source must use ${CANONICAL_BASE}${recipeId}`);
    }
  }
  return errors;
}

# 「一锅出」上线部署说明

**本地用法不变**：双击 `start.command` 即可（HOST / PORT / 限流 都有本地默认值，无需配置）。
下面是把它放到公网（网站 + 手机）时的步骤。

## 架构
- **前端**：`今天吃什么-redesign.html`（一个静态文件）
- **后端**：`ai_proxy.py`（持你的 DeepSeek key，替所有用户转发请求）
- **手机**：前端做成 PWA（加到主屏），不需要原生 App

## 1. 部署后端 proxy（持 key 的那台）
选 Render（有免费档）/ Railway / 一台便宜 VPS，任选其一。以 Render 为例：
1. 新建 Web Service，上传 `ai_proxy.py` + `requirements.txt`（或连仓库）
2. Start command：`python3 ai_proxy.py`
3. 环境变量（Environment）：
   - `DEEPSEEK_API_KEY` = 你的 key（**只放这里，永远别进前端、别提交进仓库**）
   - `HOST` = `0.0.0.0`
   - `ALLOW_ORIGIN` = 你的前端域名，如 `https://yiguochu.pages.dev`（多个用逗号分隔）
   - `RATE_LIMIT` = `50`（每个 IP 每天最多 50 次，按你愿意付的 AI 账单调）
4. 部署后得到一个 https 域名，如 `https://yiguochu-proxy.onrender.com`

## 2. 部署前端（网站）
1. 打开 `今天吃什么-redesign.html`，把脚本顶部的
   `const PROXY_BASE = 'http://localhost:8765';`
   改成你上一步的域名：`const PROXY_BASE = 'https://yiguochu-proxy.onrender.com';`
2. 把这个 html 丢到 Cloudflare Pages / Vercel / Netlify（静态托管，免费），得到 https 网址

## 3. 手机（PWA，不用做原生 App）
仓库已带 PWA 文件并接好：`manifest.json` / `icon.svg` / `sw.js`。部署时**把它们和入口 html 一起传**。
- 把入口 html 命名为 `index.html`（manifest 的 start_url 是 `./`、SW 也缓存 `./`）。
- 手机浏览器打开你的 https 网址 → 「添加到主屏幕」(iOS Safari) / 「安装应用」(Android Chrome) → 全屏带图标，像 App。
- **语音输入此时才可用**（Web Speech API 需要 https；本地 file:// 用不了）。
- iOS 图标想更精致：导出一张 180×180 的 `icon-180.png`，把页面里的 apple-touch-icon 指向它（可选）。
- 真要上应用商店：用 **Capacitor** 把这个网页包成可提交的 App（最省力），或做原生（大工程）。v1 用 PWA 足够。

## 注意
- 限流是单实例内存计数（重启清零、多实例不共享）——v1 单实例够用；要规模化再换共享存储（Redis 等）。
- `.env` 只在本地用；线上用 host 的环境变量，key 不进代码库、不进前端。
- **你（运营方）为每次生成付 DeepSeek 的钱**，所以 `RATE_LIMIT` 一定要开。

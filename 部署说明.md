# 「一锅出」上线部署说明

更新时间：2026-07-28

> Pantry Planner V2 目前只在 Draft PR 与 `recipe-validation` Preview 中验证，**未部署到 production**。本文下面的命令不表示已获得 production 发布授权。

2026-07-28 已有 Preview 基线：`direct-recommend-723a106` 的 `/plan-meal` 100 次性能门通过。随后真人浏览器旅程发现候选语义重复、烹饪顺序、基础油盐、空输入与无替代路径等问题，当前修订仍须重新完成全量测试、Preview 构建核对、线上性能门与真人浏览器复测，因此继续禁止发送 Pilot 链接。DeepSeek key 轮换由密钥持有人安排在 production 上线前完成，不阻塞内部 Preview，但仍是 production 硬门。

## 当前线上地址

- 手机入口：<https://yiguochu.pages.dev/>
- 健康检查：<https://yiguochu.pages.dev/health>
- 生成接口：`POST https://yiguochu.pages.dev/generate-meal`
- 成本熔断 KV：`RATE_KV` 已绑定到 Cloudflare KV namespace `yiguochu-rate`

现在是 **Cloudflare Pages + Pages Functions** 同源部署：

- 前端：`index.html` / `manifest.json` / `sw.js` / `icon.svg` / `icon-*.png`
- 云端代理：`tools/build-dist.mjs` 构建时把 `worker/src/worker.js` 生成到 `dist/_worker.js`
- 规划器资产：`tools/data/ingredient-taxonomy.v1.json`、`tools/data/meal-templates.v2.json`、`tools/data/ratio-rules.v1.json`；构建脚本会将它们与菜谱库一起收入发布包
- 密钥：`DEEPSEEK_API_KEY` 只存在 Cloudflare Pages secret 里，不进前端、不进仓库
- 手机：Safari / Chrome 打开线上地址后，可以添加到主屏幕，当 PWA 用
- 本地记录：只保存用户下次还会用到的场景、份数、常用食材和忌口；不再追加每日菜谱、周营养或次日反馈。旧版本历史仍保留在原设备中，但新版不读取或更新。

## 本地用法

本地不变：双击 `start.command`。

`start.command` 会同时启动 `ai_proxy.py` 和 `http://localhost:8081` 静态页面。本地页面只请求 `http://localhost:8765`；线上页面按运行时来源请求同源 `/generate-meal`。

## 重新部署

### Phase A 菜谱验证预览（当前唯一允许路径）

Phase A **只能部署到非 `main` 的 `recipe-validation` preview branch**；不得部署或提升到 production `main`。开始前必须运行菜单总表专用检查、聚合门禁和 Planner V2 真实旅程，且既有离线门禁、30/30 本地 live smoke 和移动端旅程均通过。这些自动化结果不等于真实 DeepSeek live 验证或人工菜谱批准。Wrangler 的 `--commit-message` 必须使用 ASCII，避免 Cloudflare 的 UTF-8 commit message 错误。

在项目根目录执行：

```bash
node tools/build-menu-master.mjs --check
node tools/build-regional-atlas.mjs --check
node tools/build-northeast-stew-research.mjs --check
node tools/build-jiangnan-rice-research.mjs --check
node tools/build-shandong-one-pot-research.mjs --check
node tools/build-central-plains-noodle-research.mjs --check
node tools/build-middle-yangtze-main-meal-research.mjs --check
node tools/check-recipes.mjs
node tools/run-pantry-planner-v2-journeys.mjs
node tools/run-direct-recommend-shadow.mjs
BUILD_ID="preview-$(git rev-parse --short HEAD)"
node tools/build-dist.mjs --out-dir dist --build-id "$BUILD_ID" --planner-rollout direct-recommend
npx wrangler pages deploy dist --project-name yiguochu --branch recipe-validation --commit-dirty=true --commit-message "recipe validation preview"
```

若有意修改菜谱库、区域研究账本或验证案例账本，先显式更新审阅产物，再运行上述检查：

```bash
node tools/build-menu-master.mjs --write
node tools/build-regional-atlas.mjs --write
node tools/build-northeast-stew-research.mjs --write
node tools/build-jiangnan-rice-research.mjs --write
node tools/build-shandong-one-pot-research.mjs --write
node tools/build-central-plains-noodle-research.mjs --write
node tools/build-middle-yangtze-main-meal-research.mjs --write
```

`tools/data/menu-master-baseline.v1.json` 同时锁定 Phase Zero 的生产菜谱 ID/状态、研究候选 ID 和汇总数量（当前为 72 / 12 / 60 / 24 / 0 / 72）。删除现有菜谱、改动状态或调整这些数量会使 `--write` 与 `check-recipes` 失败；只有完成明确的基线审阅后，才可以有意更新该文件并重新生成产物，不能把它当作普通计数修复。

菜单总表、全国地域地图、东北铁锅炖、江南菜饭、山东家庭主餐、中原面食与长江中游主餐研究审计仅供人工与机器审阅；不会复制到 `dist/`，也不参与运行时菜单选择。地域地图的源账本 `tools/data/regional-atlas.v2.json`、`tools/data/regional-menu-mappings.v1.json` 及生成产物 `tools/generated/regional-atlas.v2.json`、`docs/regional-atlas.md`，东北研究源账本 `tools/data/northeast-stew-research.v1.json`，江南研究源账本 `tools/data/jiangnan-rice-research.v1.json`，山东研究源账本 `tools/data/shandong-one-pot-research.v1.json`，中原研究源账本 `tools/data/central-plains-noodle-research.v1.json`，以及长江中游研究源账本 `tools/data/middle-yangtze-main-meal-research.v1.json` 和各自生成的 JSON、Markdown 审计表，均不进入发布包；它们不会改变 Planner、Worker、前端或 DeepSeek 生成行为，也不代表新增菜谱或人工批准。

地域能力账本版本为 `regional-menu-mappings-v1-20260726-r2`，覆盖 atlas 12/12 个技法家族；它记录 `full` / `partial` / `none`、运行时模板、候选模板和 blocker。当前 Draft 已记录普通熟米汤饭的完整能力，以及小米、块茎、熟豆软烩锅的部分家庭能力；后者不声称地方传统复刻。账本本身仍不进入 `dist/`。

部署输出必须是 `https://*.yiguochu.pages.dev` 预览地址，且不得等于 production `https://yiguochu.pages.dev`。

如果第一次部署或 secret 需要重设：

```bash
awk -F= '/^DEEPSEEK_API_KEY=/{print $2}' .env | npx wrangler pages secret put DEEPSEEK_API_KEY --project-name yiguochu
```

## 验证

Phase A 对预览地址验证；`/health` 必须同时满足 `recipeLibrary: "ok"`、`plannerAssets: "ok"`、`recipeFamilies: 21`、`baseRecipes: 72`（12 道人工批准 + 60 道自动闸门通过待人工评审），以及以下规划器元数据：

- `buildId` 必须等于本次干净工作区的构建 ID，并与 `/build-meta.json`、页面 `window.__YIGUOCHU_BUILD_META__` 完全一致
- `plannerRollout: "direct-recommend"`；缺失或非法 `build-meta.json` 时必须诚实回退为 `buildId: null`、`plannerRollout: "off"`
- `plannerVersion: "pantry-planner-v2"`
- `templateCatalogVersion: "templates-v2-20260728-r13"`
- `ingredientTaxonomyVersion: "taxonomy-v1-20260728-r10"`
- `ratioRulesVersion: "ratio-rules-v1-20260728-r6"`
- `activeTemplates: 11`
- `plannedTemplates: 5`

即当前 Draft 目标基线为 **11 个 active templates，5 个 planned templates**，72 道 evidence recipes 不变，Planner V2 真实旅程门禁为 138/138。该基线尚未部署 Preview 或 production；这些数字只描述 Draft PR 中待验证的构建内容。

不带 `--planner-rollout direct-recommend` 的构建默认写入 `plannerRollout: "off"`。production 继续冻结，不得把 Preview 的 rollout 构建提升或部署到 `main`。

如任一规划器资产缺失或校验失败，`plannerAssets` 必须诚实报告 `unavailable`，版本为 `null`、模板数为 `0`。同时用预览 URL 作为 `Origin` 检查 `Access-Control-Allow-Origin` 是否精确反射：

```bash
PREVIEW_URL="https://<preview>.yiguochu.pages.dev"
curl -sS "$PREVIEW_URL/health"
curl -i -sS -H "Origin: $PREVIEW_URL" "$PREVIEW_URL/health"
node tools/run-direct-recommend-preview-gate.mjs \
  --url "$PREVIEW_URL" \
  --build-id "$BUILD_ID" \
  --samples 100
curl -sS -X POST "$PREVIEW_URL/generate-meal" \
  -H 'Content-Type: application/json' \
  --data '{"meal_name":"这次的一锅主餐","targets":{"servings":2,"kcal":1300,"p":50,"fb":16},"constraints":{"purpose":"quick","servings":2,"pantry":["鸡蛋","西兰花"],"dislikes":[]}}'
node tools/run-recipe-live-scan.mjs \
  --base-url "https://recipe-validation.yiguochu.pages.dev" \
  --purpose normal \
  --concurrency 2 \
  --report /tmp/yiguochu-recipe-live-scan.json
```

`run-direct-recommend-shadow.mjs` 是零 DeepSeek 的确定性影子对照：固定运行 30 条家庭输入，同时复用 Planner 与旧 Worker selector，报告识别、覆盖、额外主要食材、生熟/部位状态、安全和人工争议项。它不得用旧路径“多买一种主料”得到的表面覆盖自动判定 V2 退步。

`run-direct-recommend-preview-gate.mjs` 只测 `/plan-meal`：先核对线上构建号和 rollout，预热 5 次，再顺序运行 100 次；任何非 JSON、JSON 解析失败、5xx/网络错误或 P95 ≥ 2000ms 都退出 1。它不调用 `/generate-plan`，因此不消耗 DeepSeek 生成预算；通过也不能替代真人浏览器点击门。

最后一条命令逐道锁定72道生产菜谱，实际调用 Preview 做端到端生成与校验；每道最多调用 DeepSeek 一次，失败不自动重试。它会消耗 API 额度，因此不并入离线单元测试，只作为菜谱批次和 Preview 发布后的显式 live 门禁。

## 菜谱来源与 Phase A exit gate

- RecipeDB 只可作为 research-only 的发现与分类参考，不得复制其数据或完整做法进生产库；GitHub 仓库的代码 license 也不等于仓库所抓取菜谱的内容许可。
- 每条 `status: "approved"` 的基础菜谱必须至少有一条 approved source，包含直达原始内容的 HTTPS `url`、`title`、`license`、`attribution`、`retrieved_at`；所有替换只能通过显式 `substitution_slots` 声明。
- 提交和部署前必须运行 `node tools/check-recipes.mjs`，失败即停止。该聚合门禁在保留菜谱、候选与晋升检查的同时，还使用权威 validator 检查 ingredient taxonomy、meal templates、Ratio DSL、`evidence_recipe_ids`、全国地域地图结构、72 道生产菜单与 24 条研究候选的地域映射，并检查地域地图及各已建立地域研究审计生成产物（含西北）是否新鲜。各地域研究轮次仍为 `research_in_progress`；门禁通过只表示结构与产物一致，不表示研究完成、运行时能力改变或生产批准。
- 不得开始 150–200 道扩展，直到：12/12 来源与 schema、100/100 静态回归、30/30 本地 live、0 allergen leak、0 未使用却列入成品的非调味食材、所有禽肉/猪肉/海鲜/鸡蛋均有熟制证据、移动端搭配依据/“这次没用”/来源署名可见、“换一换”得到不同 base recipe 或 family，并且用户批准 30 例人工评审表。
- 自动结果必须诚实解读：当前仍有 6 个 known gaps（4 个 diet 合规 + 2 个任意克数 numeric ratio）需要人工复核；100/100 静态和 30/30 live 都不能宣称这些样本已经人工批准。

## 维护提醒

- `.env` 只给本地用，不能提交。
- `dist/` 是发布产物，不提交。
- `STORE.exportForMigration()` 仍保留旧数据迁移兼容，但当前产品不上传历史记录。
- 当前有两层保护：每 IP 每天 300 次内存限流 + `RATE_KV` 全局每日预算熔断（默认 300 次/天）。用 `curl https://yiguochu.pages.dev/health` 看到 `"budget":"kv"` 才表示跨实例熔断已生效。
- 预算熔断已改为 **fail-closed**：未绑 `RATE_KV` 或 KV 读写异常时，生成接口返回 `503 budget_unavailable` 而不是放行（worker 日志有 `budget kv error` 详情）。preview 环境（所有非 `main` 分支，含 `recipe-validation`）必须在 `wrangler.toml` 绑定同一个 KV namespace，否则 preview 一律 503：

  ```toml
  [env.preview]

  [[env.preview.kv_namespaces]]
  binding = "RATE_KV"
  id = "5c29cff841c7496da029b43c6273e1ea"
  ```
- 残余风险：KV 计数是读-改-写、非原子，并发突发可有限超支，但单日总量级仍被钉在预算量级（Cloudflare `ratelimit` binding 只支持 10s/60s 窗口且按机房本地计数，不适合日预算，故保留 KV 方案）。
- 查线上 KV 计数要加 `--remote`，例如：`npx wrangler kv key get --remote "budget:2026-06-06" --namespace-id 5c29cff841c7496da029b43c6273e1ea`。不加 `--remote` 查的是本地 KV，会误判为空。
- DeepSeek 费用由部署方承担，公开分享前要确认限流上限。

# 「一锅出」上线部署说明

更新时间：2026-06-04

## 当前线上地址

- 手机入口：<https://yiguochu.pages.dev/>
- 健康检查：<https://yiguochu.pages.dev/health>
- 生成接口：`POST https://yiguochu.pages.dev/generate-meal`

现在是 **Cloudflare Pages + Pages Functions** 同源部署：

- 前端：`index.html` / `manifest.json` / `sw.js` / `icon.svg` / `icon-*.png`
- 云端代理：发布时把 `worker/src/worker.js` 复制为 `dist/_worker.js`
- 密钥：`DEEPSEEK_API_KEY` 只存在 Cloudflare Pages secret 里，不进前端、不进仓库
- 手机：Safari / Chrome 打开线上地址后，可以添加到主屏幕，当 PWA 用
- 本地记录：用户点「就吃这个」后，会在本机 `localStorage.yiguochu_v1` 里保存完整菜谱快照，后续加账号时可迁移

## 本地用法

本地不变：双击 `start.command`。

本地页面默认请求 `http://localhost:8765`，所以本地调试仍然走 `ai_proxy.py`。线上发布目录会把代理地址改成同源空字符串，让页面请求 `/generate-meal`。

## 重新部署

在项目根目录执行：

```bash
rm -rf dist
mkdir -p dist
cp index.html manifest.json sw.js icon.svg icon-180.png icon-192.png icon-512.png dist/
cp worker/src/worker.js dist/_worker.js
python3 - <<'PY'
from pathlib import Path
p = Path('dist/index.html')
s = p.read_text(encoding='utf-8')
s = s.replace(
    "const PROXY_BASE = (typeof window !== 'undefined' && window.YIGUOCHU_PROXY) || 'http://localhost:8765';",
    "const PROXY_BASE = (typeof window !== 'undefined' && window.YIGUOCHU_PROXY) || '';",
)
p.write_text(s, encoding='utf-8')
PY
npx wrangler pages deploy dist --project-name yiguochu --branch main --commit-dirty=true
```

如果第一次部署或 secret 需要重设：

```bash
awk -F= '/^DEEPSEEK_API_KEY=/{print $2}' .env | npx wrangler pages secret put DEEPSEEK_API_KEY --project-name yiguochu
```

## 验证

```bash
curl -sS https://yiguochu.pages.dev/health
curl -sS -X POST https://yiguochu.pages.dev/generate-meal \
  -H 'Content-Type: application/json' \
  --data '{"meal_name":"今日推荐","targets":{"kcal":1800,"p":60,"fb":25,"ca":800},"constraints":{"pantry":["鸡蛋","西兰花"],"dislikes":[]}}'
```

## 维护提醒

- `.env` 只给本地用，不能提交。
- `dist/` 是发布产物，不提交。
- 未来做账号系统时，新版前端可以读取 `STORE.exportForMigration()`，在用户同意后把本机历史上传到后端。
- 当前限流是 Worker 内存计数，够 v1 自用和小范围试用；如果给很多人用，需要改成 KV / D1 / Redis 这类共享限流。
- DeepSeek 费用由部署方承担，公开分享前要确认限流上限。
